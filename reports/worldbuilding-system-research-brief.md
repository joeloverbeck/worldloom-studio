# Research brief — Causal Canon Worldbuilding System, version-1.0 release pass (ninth-iteration repair half)

*This brief is the complete instruction set for the session that will perform this pass. The executor is a **Claude Code session with direct read/write access to the repository working tree** — there is no manifest to upload and no URL fetching; you read the files with your own tools and edit them in place. You have none of the context in which this brief was authored; everything you need is in this brief plus the repository itself. Produce the work directly; do not interview or ask clarifying questions.*

---

## 1. Context

You are working inside the `joeloverbeck/worldloom-studio` repository — a web app to create and maintain fictional worlds from a continuity and causality perspective. The subject of this task is the repository's **Causal Canon Worldbuilding System**, a storage- and software-agnostic worldbuilding methodology living entirely under `docs/worldbuilding-system/**`. It is currently at **version 0.8** and comprises **58 files**: 24 numbered documents (`00`–`23`), a package `README.md`, `manifest.json`, `operating_card.md`, 12 `checklists/`, and 19 `templates/`.

**Baseline.** This brief was authored against commit `b2580da` (full SHA `b2580daf44a8173509ffbad319725d84327dba95`, "Second field trial."), the repository HEAD on `main` at authoring time. Verify HEAD when you start (`git rev-parse HEAD`); if it has moved past `b2580da`, work from the live tree and note the divergence in your overhaul notes. You edit the working tree directly — the baseline pin exists so provenance is honest, not because you fetch anything from it.

**Lineage.** Iterations 1–6 (ChatGPT-Pro) and 7–8 (local Claude Code sessions) were document-editing passes; iteration 8 (version 0.8) was the first *evidence-bounded* pass, consuming the first field trial's entire finding backlog. Its changelog is the package's own `docs/worldbuilding-system/00_overhaul_notes.md` (which you will rewrite as this pass's record), and its adversarial self-assessment is `reports/ninth-iteration-outlook.md`, which concluded that another editing pass was **not** warranted, that the highest-value ninth iteration was a **second field trial run cold on 0.8**, and that a defensible 1.0 gate was: *"a second field trial, run cold on 0.8, whose friction log demands no structural change and confirms the 0.8 additions carry their weight."*

That trial has now happened — it is the ninth iteration's first half, and this pass is its second. Its log — **`reports/ninth-iteration-field-trial.md`** — is your **primary seed and work order**. A steward with no authorship stake in 0.8 built the world *Carillon* cold through the 0.8 README path (kernel → decomposition → audit → ledger-based admission → propagation → the first-ever field runs of the temporal (`09`) and institutional/suppression (`12`) passes → a supersession chain two repairs deep (C-1, C-2) → QA with a live mystery-preservation firing), logging three frictions (F2-01…F2-03), two questions (Q2-01, Q2-02), six skips (S2-01…S2-06), and twenty-three validations (V2-01…V2-23). Its verdict: **version 0.8 works, the eighth iteration's repairs specifically work, and the outlook's convergence gate is met** — no finding touches doctrine, every friction is a sentence, and the trial's own recommendation 2 is to **declare 1.0 with a named coverage statement**. The user has accepted that recommendation: this pass's mandate is to produce version 1.0. The trial's filled instruments live in `reports/field-trial-world-2/` (13 files).

**Equivalence of the trial's evidence to your tree.** The trial log pins package version 0.8 at commit `c945744`. Verified at authoring time: `git diff --stat c945744 b2580da -- docs/worldbuilding-system/` is **empty** — the package is byte-identical between the commit the trial ran against and this brief's baseline (the only changes in that range are the trial log and the `reports/field-trial-world-2/` artifacts). Every claim the trial makes about the documents therefore applies verbatim to the tree you are editing.

**Package facts you can rely on** (from the version-0.8 `00_overhaul_notes.md` retention audit, committed at the trial's own commit and unchanged at baseline; spot-verified against `README.md` and `manifest.json` at baseline): 12 operating laws in `01`; 13 truth layers, 11 canon statuses, 12 admission decision operations, 12 repair operations, and the record-lifecycle doctrine in `03`; fourteen propagation domains in `04`; two explicitly mapped severity vocabularies in `06`; 28 core QA tests plus the P1/P2 package self-audit tests, all with calibrated anchors, in `18`; 74 glossary entries in `22`; `manifest.json` at `version` `0.8`, `iteration` 8, `file_count` 58.

## 2. Read in full (authority order)

Read everything below, in this order, before changing anything. The evidence tier comes first — your priorities are not self-derived; they are in the trial log, and they are small.

**Evidence and lineage (the mandate):**

- `reports/ninth-iteration-field-trial.md` — **the work order.** The F2/Q2/S2/V2 ledgers, the convergence verdict against the outlook's gate, the honestly-untested list, and the four ranked recommendations that define this pass.
- `reports/ninth-iteration-outlook.md` — the eighth iteration's adversarial self-assessment: the 1.0 gate this trial has now satisfied (§2), the least-confident 0.8 fixes the trial went on to validate (§1.1–1.2), the standing agnosticism risks (§1.5 — carry these into your own outlook), and the deferred-but-real collaboration trial (§3).
- `docs/worldbuilding-system/00_overhaul_notes.md` — the iteration-8 changelog: provenance format, finding→disposition table shape, fate mapping, retention audit, and the single-changelog doctrine ("iteration history lives here, not in the doctrine files") that your pass must also honor. You rewrite this file as this pass's record.
- `reports/eighth-iteration-field-trial.md` and `reports/eighth-iteration-outlook.md` — the first trial and iteration 7's self-assessment: prior evidence tier only. The convergence criteria your outlook re-scores originate in the eighth-iteration outlook §2 and were re-scored once already in the ninth-iteration outlook §2; your outlook closes that scoring lineage.

**Trial artifacts — the Carillon world** (`reports/field-trial-world-2/`, 13 files; evidence behind every F2/Q2 item and the V2 validations — read all of them, priority on the exhibits): `world_kernel.md`, `seed_decomposition.md`, `frontloaded_seed_audit.md`, `admission_ledger.md` (three addendum batches, one promotion — V2-08/V2-09), `fact_cards.md` (Card 5 carries the two-deep supersession History — V2-18), `capability_card_intercalation.md` (in-place additive amendment — V2-20), `mystery_ledger.md`, `propagation_report_S2a.md`, `temporal_pass.md` and `institutional_suppression_pass.md` (**the improvised pass-report containers behind Q2-01 and the appended ledger rows behind F2-01**), `contradiction_report_C1.md`, `contradiction_report_C2.md`, `qa_regression_profile.md` (**the 28-row expanded worksheet behind F2-02**, plus the S3a severity wobble behind F2-03 and the Level-5 derivation behind Q2-02).

**Primary (the package you are releasing)** — all 58 files, in the package's own authority order. A release pass certifies the whole tree, so read everything; the files receiving edits are flagged:

Doctrine & primitives — `README.md` (**edit: version 1.0 + coverage statement**, §7A), `01_core_theory.md`, `02_world_model.md`, `03_truth_layers_and_canon_governance.md`, `04_domain_atlas.md` — **all four numbered doctrine files stay byte-identical** (§3.2); read them to certify nothing you write elsewhere contradicts them.

Core protocols — `05_creation_protocol.md`; `06_canon_fact_admission_protocol.md` (**edit: F2-03 law-classification parenthetical; Q2-02 solo decision-record clause**); `07_propagation_engine.md` (**edit: F2-01 pass-generated-facts sentence**); `08_constraint_composition.md`; `09_temporal_and_timeline_protocol.md` and `12_institutional_economic_and_suppression_protocol.md` (**edit: F2-01 sentence each** — both just passed their first field runs (V2-15, V2-16); the fix is one routing sentence, not a revision); `10`, `11`, `13`–`17` — no findings; read for release certification and wiring consistency.

Quality, examples, workflow, reference — `18_quality_assurance_tests.md` (**edit: F2-02 worksheet/test-list reconciliation**); `19_worked_examples.md` (optional V2-20 one-liner, §3.3); `20_ai_assisted_workflow.md`; `21_templates_index.md` (**edit: Q2-01 pass-report sentence**); `22_glossary.md` (update only if an edit adds vocabulary — none is expected to); `23_research_notes_and_bibliography.md`; `manifest.json` (**edit: version 1.0, iteration 9**); `operating_card.md` (**edit only if you place the Q2-02 clause on its severity map** — and then re-verify the card against its parents, since it is a derived document; the trial's V2-02 drift check found 0.8 in sync).

Instruments — all 12 `checklists/` and all 19 `templates/`: no trial findings against any of them this time (the ledger, kernel, gate, audit, mystery-ledger, contradiction-report and preservation instruments were all validated in use — V2 ledger); read for certification, edit none unless an F2/Q2 fix's wording forces a cross-reference touch, argued in the changelog.

**Boundary-awareness (read to bound scope, not a conformance or editing target):**
- `README.md` (repository root) — one-line repo identity; the downstream web app is out of scope. Do not edit it; do not let web-app framing leak into the methodology.

Nothing else in the repository (`.claude/`, `docs/agents/`, older files in `reports/`, `CLAUDE.md`) is part of this task; do not edit any of it except the single new report named in §7C. `reports/ninth-iteration-field-trial.md`, `reports/ninth-iteration-outlook.md`, both eighth-iteration reports, and `reports/field-trial-world/**` and `reports/field-trial-world-2/**` are **read-only evidence** — never edit them.

## 3. Settled intentions

These decisions are final. Lines marked `assumption:` were resolved by the brief author's judgment when the user was unavailable — treat them as defaults the user could have overridden, and honor them unless the user has said otherwise in your session.

1. **This is a release pass, not another overhaul.** Two consecutive trials have converged: frictions are sentences, instruments generate value, doctrine holds. The trial's recommendation 1 is your entire substantive work list: **apply the log's sentence fixes and nothing else.** The five fixes, with the trial's own proposed shapes:
   - **F2-01** — one sentence in `07`, `09`, and `12`: new facts a sweep or pass surfaces go through admission like any other fact (the route `13` already states for repair-created facts, generalized to pass-generated facts).
   - **F2-02** — reconcile `18`'s 9-row condensed scorecard worksheet with its 28 named tests: either present the worksheet per-test or state that the 9 rows are a triage view (the trial steward had to expand it to 28 rows to apply the n/a convention and per-test anchors).
   - **F2-03** — a parenthetical in `06`'s severity levels: classify a law by the scope of what it structures, not by its type (a world-structuring law is not inherently Level 2 just because "a law" appears there).
   - **Q2-02** — a clause in `06` or on the operating card's severity map: when a solo steward's Level-5 change owes an explicit decision record (the work scale assigns it to catastrophic work only; make the derivation readable instead of derivable).
   - **Q2-01** — one sentence in `21`: where a specialized pass's output lives — sanction the pass-report file (sweep results + domain cards bundled, on the model of the propagation report) that the trial steward improvised as `temporal_pass.md` / `institutional_suppression_pass.md`.
   You may tune each fix's exact wording and placement (the trial proposes, the protocol tier disposes), but you may not widen scope: no other substantive change to any protocol, checklist, or template. If implementing a fix exposes a genuine adjacent defect, you may repair it only with the discovery argued in the changelog — the bar is *defect*, not *improvement*.
2. **Doctrine (`01`–`04`) stays byte-identical.** No F2/Q2 item touches it, and leaving it untouched is precisely what satisfies the outlook's remaining convergence criterion 1. This is a hard constraint, not a preference: if you believe a fix requires amending doctrine, you have misread the fix — state the tension in the changelog and place the fix at the protocol/instrument tier where the trial located it.
3. **Optional micro-fix, at your discretion:** V2-20's parenthetical suggests a worked one-liner in `19`'s records-lifecycle example showing that an *additive* amendment owes no History note (only outgoing wording does). It is log-recorded but not in the ranked recommendations; you may add the one line or decline it — either way, table the disposition in the changelog.
4. **Declare version 1.0 with a named coverage statement** (trial recommendation 2). The version markers — package `README.md`, `manifest.json`, `00_overhaul_notes.md` — move to `1.0`; `manifest.json`'s `iteration` becomes 9 (this pass completes the ninth iteration, whose first half was the trial). The README's version framing must state plainly, as the trial's verdict does, **which surfaces are field-tested** (the core pipeline twice over, constraint composition (`08`), temporal (`09`), institutional/suppression (`12`), the record lifecycle under a two-deep supersession chain, batch admission with promotion, and the QA + mystery-preservation machinery under a live firing) **and which are not** (`10`, `11`, `14`, `16`, `17` never run in anger; `15` and every multi-steward mechanism; `19`/`20` under a genuinely naive steward; the aesthetic-promise disposal in a lyrical- or sacred-primary world). 1.0 is an honest claim about tested surfaces, not a claim of total coverage — the coverage statement is what makes the version number true. Keep it proportionate: a short subsection, not a new document.
5. **No tenth editing iteration is pre-commissioned.** The trial's recommendation 3 ("do not run a tenth editing iteration — further self-derived editing is the retired failure mode") is the evidence-backed default. Your outlook (§7C) must still reason the next-iteration verdict fresh and adversarially, but you are not licensed to *perform* any work beyond §3.1–§3.4 in this pass regardless of that verdict.
6. **Negative settled intentions — do not re-open:** no severity-scale unification (two mapped vocabularies are permanent absent field failure); no reinforcement-tail weaving; no new files and no removals (58 stays 58); no growing `manifest.json` into a schema; no syncing the package's Saltmarrow example in `19` with the frozen trial artifacts in `reports/` (they are canonical for different things and must be left to diverge); no storage/software/API/web-app/LLM coupling anywhere.
7. **No-downgrade now covers both trials' validated strengths.** The iteration-8 protected set (the V-01…V-19 behaviors listed in the 0.8 retention audit) remains protected, and this pass adds the V2 ledger: the routed reading path (V2-01), the operating card's fidelity to its parents (V2-02), the reconciled kernel (V2-03), the admission ledger and its Promotions rule (V2-08/V2-09), the division-of-labor and digest rules (V2-10), the record lifecycle under accumulation (V2-18/V2-19), the n/a convention and P1/P2 placement (V2-21), and the mystery-preservation boundary logic (V2-23). Your five sentence fixes sit adjacent to several of these; the validated behavior is the acceptance test for any wording you touch.
8. **Single-changelog discipline — do not re-stratify.** Fixes are woven in timelessly, never as "version 1.0 adds…" strata; iteration history lives only in `00_overhaul_notes.md`. The only current-version markers are the package `README.md`, `manifest.json`, and `00`.
9. **The operating card is a derived document.** If any parent content you edit (`06`'s severity material, `21`'s minimal paths) is condensed on the card, re-synchronize the card and say so in the changelog; if you place the Q2-02 clause on the card itself, its governing parent (`06`) must carry or explicitly delegate the rule — the card never becomes an authority tier.
10. **`assumption:` The closing report is `reports/tenth-iteration-outlook.md`** — continuing the lineage convention where the outlook filename names the next iteration it informs (iteration 7's pass wrote `eighth-iteration-outlook.md`; iteration 8's wrote `ninth-iteration-outlook.md`; this pass, completing iteration 9, writes the tenth). The user can rename on receipt.
11. **`assumption:` Leave all changes uncommitted.** Do not run `git commit`. The user reviews the working tree and commits themselves, as they did for every prior iteration.

## 4. The task

Perform the **version-1.0 release pass** of the Causal Canon Worldbuilding System — the repair half of the ninth iteration (target type: foundational / doc-overhaul, evidence-bounded and deliberately minimal), executed directly on the repository files. Read the trial log, the outlook, and the full package (§2); apply the five sentence fixes exactly as scoped in §3.1 (plus, at your discretion, §3.3); declare version 1.0 with the named coverage statement (§3.4); rewrite `00_overhaul_notes.md` and `manifest.json` as the version-1.0 stewardship record with honest local-session provenance and a complete finding→disposition mapping. Finally, deliver the tenth-iteration outlook (§7C): the warnings and weaknesses of version 1.0 as released, a closing re-score of the convergence criteria, and a reasoned, adversarial verdict on whether a next iteration is warranted and of what shape.

## 5. Exploration + online-research mandate

Explore the repository as deeply as needed beyond the files listed above. Research online as deeply as needed — but this pass is evidence-bounded by design, and its fixes are sentences; expect to need little or none. If an external lane genuinely sharpens a fix (e.g., versioning/release-notes practice for the coverage statement), cite it in `23_research_notes_and_bibliography.md` and the overhaul notes' research rationale; `23`'s own translation rules forbid citation as decoration, and "no research was needed" is an acceptable — likely correct — rationale.

## 6. Doctrine & constraints

Honor these, all derived from the package's own established doctrine and the settled intentions above:

- **Authority flows downward.** `01`/`03` govern everything below; `02`/`04` provide primitives; protocols `05`–`17` turn doctrine into craft; `18`–`23` provide quality control and reference; `checklists/` and `templates/` operationalize the protocols. This pass touches only the protocol/instrument tiers (§3.2); every fix must be *consistent with* the doctrine above it — F2-01's sentence, in particular, generalizes a route `06` and `13` already own, so state it as a pointer to the admission protocol's jurisdiction, never as new admission machinery inside `07`/`09`/`12`.
- **Every substantive change traces to a logged trial finding** (F2-01…F2-03, Q2-01, Q2-02, optionally V2-20) **or to a defect you discovered and argued in the changelog.** No unprompted improvement fronts (§3.1); no doctrine edits (§3.2).
- **The separations stay crisp:** canon status / constraint tag / admission decision operation / repair operation / consequence mode / preservation boundary. The trial scored vocabulary separation 3/3 under heavier stress than trial 1 applied; do not re-blur it. F2-01 and Q2-01 both sit on jurisdiction seams (sweep output → admission; pass output → record) — word them so the sweeps' existing "never change canon from inside a sweep" red flags remain exactly true.
- **Instruments stay wired to parent protocols.** Every checklist and template keeps its parent-protocol note, completion rule, and red flags; `21`'s matrix must still match the actual instrument set (unchanged at 19 templates); the Q2-01 sentence sanctions a *record container*, not a new instrument family — if you find it needs a template, you have exceeded the finding.
- **Vocabulary stays authoritative and consistent** with `22_glossary.md`; none of the five fixes should need new glossary entries — if one does, update the glossary and every use site in lockstep and argue it.
- **Consequence doctrine stays mode-aware, and the institutional edge stays hard.** `12` receives one routing sentence and nothing else.
- **Agnosticism guard** (§3.6): no coupling introduced anywhere; **no package file cites a `reports/…` path**; the coverage statement names *protocol surfaces*, never repository trial artifacts.
- **No re-stratification** (§3.8): fixes woven in timelessly; history only in `00`.

## 7. Deliverable specification

Three things. A and B are in-place edits; C is one new file outside the package.

**A. The released package, edited in place** — `docs/worldbuilding-system/**` after your changes:
- the five §3.1 fixes applied (and §3.3 dispositioned), each traceable and compliant with §6;
- `README.md` at version `1.0` with the named coverage statement (§3.4), the path and package map otherwise preserved (the trial validated them literally — V2-01);
- `manifest.json` at `version` `1.0`, `iteration` 9, `file_count` 58, date updated, local-session provenance, no stale commit strings;
- `01`–`04` byte-identical to baseline; every file not named in §2's edit flags either untouched or touched only by an argued cross-reference.

**B. `docs/worldbuilding-system/00_overhaul_notes.md` — rewritten as the version-1.0 changelog:**
- honest local-session provenance: executor (Claude Code + your model), the HEAD actually worked from, direct file reads, edits uncommitted;
- the evidence base: the second field trial as work order, the convergence gate it satisfied, and the **complete finding→disposition table** — every F2-01…F2-03, Q2-01, Q2-02, S2-01…S2-06 item and each of the trial's four ranked recommendations, marked fixed-as-proposed / fixed-differently-because / declined-because / no-change-needed, with file pointers (the S2 skips largely resolve to "no change — legible skip per scope", and recommendation 4's future-trial list routes to your outlook, not to package edits);
- a **fate mapping** covering every version-0.8 file (the overwhelming majority: "untouched — release-certified");
- a **retention audit** proving no version-0.8 content was lost and both trials' validated strengths (§3.7) survive;
- the **coverage statement of record** (the README carries the user-facing version; the changelog carries the full field-tested / honestly-untested accounting);
- the research rationale (§5 — likely "none needed", stated plainly).

**C. `reports/tenth-iteration-outlook.md` — new, outside the package** *(assumption: this filename; the user can rename on receipt)*, plus a closing chat message summarizing it. This report is **always produced**, with its determination embedded as a section — it is the report the user asked for: the possible warnings and weaknesses of version 1.0, and whether a next iteration is actually warranted after the changes. It must contain:
- the **warnings and weaknesses of version 1.0 as released**: what 1.0 declares tested versus what remains honestly untested (`10`, `11`, `14`, `16`, `17`; `15` and all multi-steward machinery; `19`/`20` under a naive steward; the aesthetic-promise disposal outside institutional/material-primary worlds); the risk that a 1.0 label invites uses the coverage statement excludes; the operating card as a standing derived-document drift liability; the two-trials-one-model-family contamination caveat both trial logs disclose; the standing agnosticism risks (the ninth-iteration outlook §1.5 — manifest-as-schema, operating-card productization, the twin Saltmarrow copies); and any tension your own five fixes created — each with reasoning;
- a **closing convergence re-score**: the four criteria from `reports/eighth-iteration-outlook.md` §2, as re-scored by the ninth-iteration outlook §2 and now finally scored against the second trial plus this pass — state plainly whether each is met and whether the 1.0 gate the ninth-iteration outlook defined was satisfied at release;
- a clear, reasoned **verdict on whether a next iteration is warranted** — yes/no plus why, and if yes its highest-value shape. The evidence-backed default is the trial's own recommendations 3 and 4: no further self-derived editing pass; future work, if any, is *field trials in priority order* (a sacred- or lyrical-primary world; a genuinely naive-steward trial; the collaboration trial when a second real contributor exists), each of which would generate its own evidence-bounded repair pass only if its log demands one. Since this outlook is written by the session that just performed the release, be adversarial with yourself: the verdict must not be a self-grade inflated by the satisfaction of shipping 1.0.

**Do not commit** (§3.11). Leave the working tree for the user's review, and make the closing chat message a faithful summary of the outlook report plus a plain statement of what changed in the package.

**Locked / no-questions.** Do the work directly. Do not interview, do not ask clarifying questions — the requirements above are final. If a genuine contradiction makes a requirement impossible, state it in `00_overhaul_notes.md` and the outlook report and proceed with the most faithful interpretation.

## 8. Self-check (run against the working tree before your closing message)

- Every trial ledger item (F2-01…F2-03; Q2-01, Q2-02; S2-01…S2-06) and each of the four ranked recommendations has an explicit disposition in `00_overhaul_notes.md`, with file pointers; every substantive change traces back to one of them or to an argued discovered defect; the V2-20 micro-fix is dispositioned either way.
- `git diff` for `01_core_theory.md`, `02_world_model.md`, `03_truth_layers_and_canon_governance.md`, and `04_domain_atlas.md` is empty — doctrine byte-identical to baseline.
- The five fixes read as timeless package prose (no version strata), sit at the tier the trial located them, and none blurs a jurisdiction seam: sweeps still never change canon from inside a sweep; pass-generated facts route through admission; the pass-report sentence sanctions a container, not a new instrument.
- Every version-0.8 file's fate is accounted for; both trials' validated strengths (§3.7) survive intact; no file was added or removed (58 files); `21`'s matrix and the README's package map still match the actual file set.
- `README.md` and `manifest.json` read version `1.0` (iteration 9), and the coverage statement names the field-tested surfaces and the honestly-untested list without citing any `reports/…` path; the operating card is either untouched or re-verified in sync with its parents.
- No storage/software/API/web-app/LLM coupling introduced; no package file cites a repository path; the only current-version markers are `README.md`, `manifest.json`, `00_overhaul_notes.md`.
- `reports/tenth-iteration-outlook.md` exists with the version-1.0 warnings/weaknesses, the closing convergence re-score, and an explicit next-iteration verdict with the future-trial priority list; the closing chat message summarizes it faithfully.
- Every external claim that shaped a change is cited in `23` and the overhaul notes; if none, that is stated.
- Nothing outside `docs/worldbuilding-system/**` and `reports/tenth-iteration-outlook.md` was modified by this session (verify against your own edit log, not raw `git status` — surface, don't own, any concurrent changes); all trial logs, outlooks, and `reports/field-trial-world*/**` artifacts are untouched; nothing was committed.
